/**
 * easy-performance-management 테마 — FE-CC-2 @easy/tokens 표준 직접 적용 (override 없음).
 *
 * 그린필드라 자매품 표준을 그대로 채택 (#9a-2 brandBlue + #9a-3 slate + #9a-4 600 + #9a-5 pointer).
 * jobeval 단계 4 cutover `cc1bc03` 패턴 정합.
 */
import { createEasyTheme } from '@easy/tokens';

export const theme = createEasyTheme();
