package com.easyperformance.program;import jakarta.persistence.LockModeType;import org.springframework.data.jpa.repository.*;import org.springframework.data.repository.query.Param;import java.util.*;
public interface ProgramGoalRepository extends JpaRepository<ProgramGoal,UUID>{Optional<ProgramGoal> findByIdAndTenantId(UUID id,UUID tenantId);@Lock(LockModeType.PESSIMISTIC_WRITE)@Query("select g from ProgramGoal g where g.id=:id and g.tenantId=:tenantId")Optional<ProgramGoal> findLocked(@Param("id")UUID id,@Param("tenantId")UUID tenantId);List<ProgramGoal> findAllByTenantIdAndParticipantIdOrderByCreatedAtAsc(UUID tenantId,UUID participantId);List<ProgramGoal> findAllByTenantIdAndParticipantIdIn(UUID tenantId,Collection<UUID> participantIds);}
