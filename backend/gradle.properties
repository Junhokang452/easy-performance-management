# Gradle 데몬 JVM 메모리 — OOM Kill(exit 137) 방지 (WSL2 메모리 제한 대응, 자매품 정렬: ware/hcm/jobeval).
org.gradle.jvmargs=-Xmx512m -Xms256m -XX:MaxMetaspaceSize=256m
org.gradle.daemon=true
org.gradle.parallel=false
